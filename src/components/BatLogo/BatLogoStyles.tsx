import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    title: {
        textAlign: 'center',
        fontSize: 30,
        color: '#e5bf3c',
        fontWeight: '600'
    },
    logo: {
        alignSelf: 'center',
        resizeMode: 'contain',
        height: 200
    }
})